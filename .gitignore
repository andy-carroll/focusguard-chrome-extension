# Node modules (if you add any build tools later)
# Test files are included in git for CI, but excluded from production builds via packaging script

node_modules/
!tests/.gitkeep # (optional) allow keeping the directory structure if empty


# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/

# Chrome extension packaging
*.crx
*.pem
dist/
build/

# Logs
*.log
npm-debug.log*

# Distribution directory
dist/
*.zip
